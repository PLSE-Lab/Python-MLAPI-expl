{"cells":"import numpy as np\nimport pandas as pd\nimport matplotlib.pyplot as plt\nimport sqlite3\nimport datetime\nfrom collections import Counter\n\nsql_conn = sqlite3.connect(\u0027../input/database.sqlite\u0027)\ndf = pd.read_sql(\u0027SELECT created_utc FROM May2015\u0027, sql_conn)\n#df = list(df.created_utc)\n\ncounter = dict()\nfor time in df.created_utc:\n    date = datetime.datetime.fromtimestamp(time)\n    str_rep = str(date.month) + \u0027-\u0027 + \u0027{0:02d}\u0027.format(date.day)\n    if str_rep not in counter:\n        counter[str_rep] = 0\n    counter[str_rep] += 1\n\nlabel = counter.keys()\nlabel = sorted(label)\ncounts = []\nfor l in label:\n    counts.append(counter[l])\n\nprint(len(label))\nprint(len(counts))\n\nplt.bar(list(np.arange(1, 32) - 0.2), counts, width=0.4, color=[\u0027#00ffff\u0027] * 31)\nplt.xlabel(\u0027Dates\u0027)\nplt.ylabel(\u0027Num of reddits\u0027)\nxticks = label\nplt.xticks(range(1, 32), xticks, rotation=\u0027vertical\u0027)\nplt.show()\nplt.savefig(\u0027periodic.png\u0027)\n"}